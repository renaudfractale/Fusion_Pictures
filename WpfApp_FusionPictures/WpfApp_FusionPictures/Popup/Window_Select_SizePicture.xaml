<Window x:Class="Window_Select_SizePicture"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_FusionPictures"
        mc:Ignorable="d"
        Title="Select SizePicture" Height="82" Width="300"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ComboBox Name="ComboBox_choose" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" SelectionChanged="ComboBox_choose_SelectionChanged"></ComboBox>
        <Button Name="Button_OK"  Grid.Column="0" Grid.Row="1" Click="Button_OK_Click">OK</Button>
        <Button Name="Button_Cancel"  Grid.Column="1" Grid.Row="1" Click="Button_Cancel_Click">Cancel</Button>
    </Grid>
</Window>
